(()=>{const cards=[...document.querySelectorAll('article')],search=document.querySelector('#search'),priority=document.querySelector('#priority');let category='all';function filter(){let count=0;for(const card of cards){const match=(category==='all'||card.dataset.category===category)&&(priority.value==='all'||card.dataset.priority===priority.value)&&card.textContent.toLowerCase().includes(search.value.trim().toLowerCase());card.hidden=!match;if(match)count++}document.querySelector('#count').textContent=`${count} из ${cards.length}`;document.querySelector('#empty').hidden=count>0}document.querySelectorAll('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{category=btn.dataset.filter;document.querySelectorAll('[data-filter]').forEach(b=>b.setAttribute('aria-pressed',String(b===btn)));filter()}));search.addEventListener('input',filter);priority.addEventListener('change',filter)})();
